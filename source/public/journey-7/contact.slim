---
title: 'Apply for help with fees'
nav_title: 'GOV.UK'
---

form
  .row
    .small-12.medium-6.columns
      br
      a href="address.html" Back
      span.hint &nbsp; &nbsp; Step 15 of 17
      br
      br
      h4 What's your email address?  


  .row
    .small-12.medium-6.columns
      p class="large"
        | We'll email you to confirm your application has been received. 

  .row
    .small-12.medium-6.columns
      label for='case-number'
        | Email address
        textarea rows="2" cols="20"


  
  .row
    .small-12.medium-6.columns
      .options.check
        .option
          label for="email"
            input id="email" name="email" type="checkbox" Check this box if you're willing to share your experience of this service. A member of the team may contact you. We won't use your email address for anything else. 
 
      br
      a href="summary.html" 
        .button.success Continue


javascript:
  $(document).ready( function() {
    buildNextUrl('3-savings.html');
    $('#fee').bind('change', function () {
      $("#next-url").attr("href", updateQueryStringParameter('fee', $(this).val()));
    });

    $('#email').on('change', function () {
      console.log('clicked email cb');
      $('#email-detail').toggle($('#email').is(':checked'));
      $(this).parent().toggleClass('selected', $(this).is(':checked'));
    });

    $('#phone').on('change', function () {
      console.log('clicked phone cb');
      $('#phone-detail').toggle($('#phone').is(':checked'));
      $(this).parent().toggleClass('selected', $(this).is(':checked'));
    });


  });
