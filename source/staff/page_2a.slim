---
title: 'Help with fees'
nav_title: 'Help with fees'
---
form
  br
  .row
    .small-12.medium-12.large-12.columns
      h2 Process application
  br
  .row
    .small-12.medium-12.large-12.columns
      h4 Savings

  .row
    .small-12.medium-6.large-5.columns
      .options.radio
        .option
          label for="savings"
            input id="savings" type="checkbox" name="savings"
            | Do they have more than £3,000
  #savings-more.row.hide
      .small-12.medium-6.large-3.columns
        | Test opened section
        


  .row
    .small-12.medium-6.large-5.columns    
      p
        | Do they have more than £3,000 in savings and investments?

      .options.radio
        .option
          label for="savings-no"
            input id="savings-no" type="radio" name="ben-type" value="b"
            | No
        .option
          label for="savings-yes"
            input id="savings-yes" type="radio" name="ben-type" value="a"
            | Yes

  br
  .row
    .small-12.medium-7.large-7.columns
      details
        summary How is savings worked out?
        .panel-indent
          div 
          strong If the applicant has less than £3,000 in savings
          | , they'll be able to get help with their fees, as long as they receive certain benefits or are on a low income.
          strong If they have more than £3,000 in savings
          | , xxxxxxxx
          [Colin to insert data table]
          strong If they're 61 or over
          | and have £16,000 or less in savings, they may be able to get help with their fees.

  br
  br
  .row
    .small-12.medium-7.large-7.columns
      .row
        .small-12.medium-6.large-5.columns
          a#next-url href='page_3a.html'
            .button.success Next - Assess income

javascript:
  $(document).ready( function() {

    $('input[type=radio]').on('change', function () {
      $("[name='" + $(this).attr('name') + "']").parent().removeClass("selected");
      $(this).parent().addClass('selected');
    });

    $('#proccess-application').addClass('active');
    $('#fee').bind('change', function () {
      $page = 'page_3' + getPage();
      $("#next-url").attr("href", $page+'.html?fee=' + $(this).val());
    });
    $('#savings').on('change', function() {
      console.log('clicked savings cb');
      $('#savings-more').toggle($('#savings').is(':checked'));
      $(this).parent().toggleClass('selected',$(this).is(':checked'));
    });
    $("input[name='ben-type']").on('change', function () {
      $was = $("#next-url").attr("href");
      $should_be = $was.substring(0,6) + getPage() + $was.substring(7);
      $("#next-url").attr("href", $should_be);
    });
  });

  function getPage() {
    $possible = $("input[name='ben-type']:checked").val();
    $result = $possible != 'b' ? 'a' : 'b';
    return $result;
  }
