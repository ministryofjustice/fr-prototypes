---
title: 'Help with fees'
nav_title: 'Help with fees'
section_title: 'Process payment'
rh_help: true
---
form
  br

  h2 Payment details


  br
  .row
    .small-12.medium-10.large-10.columns    
      h4 The applicant must pay <strong>£305</strong>.
      label Have you received payment for the correct amount?
    .small-12.medium-9.large-8.columns.end   
      .options.radio
        .option
          label for="payment-correct-yes"
            input id="payment-correct-yes" type="radio" name="payment-correct" value="true"
            | Yes
        .option
          label for="payment-correct-no"
            input id="payment-correct-no" type="radio" name="payment-correct" value="false"
            | No
  
  #payment-comment.hide
    .row
      .small-12.medium-9.large-8.columns
        .panel-indent
          label for='payment-comment'
            | What is incorrect about the payment?
            textarea rows="2" cols="50"
  br
  .row
    .small-12.medium-12.large-12.columns
      a#next-url href='payment-4a.html'
        .button.success Next
 

javascript:
  $(document).ready( function() {
  });

  $("input[name='payment-correct']").on('change', function () {
    $('#payment-comment').toggleClass('hide', $("input[name='payment-correct']:checked").val() == "true");
    buildNextUrl($("input[name='payment-correct']:checked").val() == "true" ? 'payment-4a.html' : 'payment-4b.html');
  });